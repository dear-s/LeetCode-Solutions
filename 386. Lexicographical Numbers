
class Solution:
    def lexicalOrder(self, n: int) -> List[int]:
        
        
        # convert into strings and sort
        
        nums = []
        
        for i in range(1, n+1):
            nums.append(str(i))
            
        nums.sort()
        
        new_arr = []
        
        for i in nums:
            new_arr.append(int(i))
            
        print("new_arr: ", new_arr)
        return new_arr
        
