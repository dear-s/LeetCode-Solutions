class Solution:
    def processStr(self, s: str) -> str:

        res = ""

        for i in s:
            if i == "*":
                res = res[:-1]
            elif i == "#":
                old = res
                res = old + old
            elif i == "%":
                res = res[::-1]
            else:
                res = res + i

        return res
